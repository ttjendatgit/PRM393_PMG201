import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Backgrounds (Stitch light palette) ────────────────────────────────────
  static const background       = Color(0xFFEEF4FF);
  static const surface          = Color(0xFFFFFFFF);
  static const surfaceLow       = Color(0xFFFFFFFF);
  static const surfaceContainer = Color(0xFFF8FAFF);
  static const surfaceHigh      = Color(0xFFF1F5FF);
  static const surfaceHighest   = Color(0xFFE8EEFF);

  // ── Brand ─────────────────────────────────────────────────────────────────
  static const primary          = Color(0xFF4F46E5);   // Indigo
  static const primaryContainer = Color(0xFFEEF0FF);   // Light indigo
  static const secondary        = Color(0xFF2563EB);   // Blue
  static const aiPurple         = Color(0xFF8B5CF6);   // AI purple

  // ── Semantic ──────────────────────────────────────────────────────────────
  static const success          = Color(0xFF16A34A);
  static const warning          = Color(0xFFFF7F50);
  static const error            = Color(0xFFDC2626);

  // ── Text ──────────────────────────────────────────────────────────────────
  static const text             = Color(0xFF172033);
  static const muted            = Color(0xFF64748B);

  // ── Borders ───────────────────────────────────────────────────────────────
  static const outline          = Color(0xFFD6E4FF);
  static const outlineVariant   = Color(0xFFE2E8F0);
}
